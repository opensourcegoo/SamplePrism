<UserControl x:Class="SamplePrism.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SamplePrism.Views"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:core="clr-namespace:SamplePrism.Core"
        xmlns:behavior="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"  d:DesignWidth="1200" d:DesignHeight="900">
    <UserControl.Resources>
        <!--<ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>-->
    </UserControl.Resources>


    <behavior:Interaction.Triggers>
        <behavior:EventTrigger EventName="Loaded">
            <prism:InvokeCommandAction Command="{Binding LoadCommand}" CommandParameter="DefaultView" />
        </behavior:EventTrigger>
    </behavior:Interaction.Triggers>
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*" />
            <ColumnDefinition Width="8.5*" />
        </Grid.ColumnDefinitions>
        <!--标题头-->
        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Height="40" Background="{DynamicResource PrimaryHueLightBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="Plane" Margin="15,0,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="25" Width="25" />
                <TextBlock Text="Prism Application" Foreground="Black" VerticalAlignment="Center" Margin="10,0,0,0" FontSize="14" FontWeight="Bold" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" WindowChrome.IsHitTestVisibleInChrome="True" >
                <Button  Style="{StaticResource MaterialDesignToolButton}" Command="{x:Static SystemCommands.MinimizeWindowCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window,Mode=FindAncestor}}">
                    <materialDesign:PackIcon Kind="WindowMinimize" HorizontalAlignment="Center" VerticalAlignment="Center"  Foreground="White" Width="20" Height="20" />
                </Button>

                <Button  Style="{StaticResource MaterialDesignToolButton}" Command="{x:Static SystemCommands.MaximizeWindowCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window,Mode=FindAncestor}}">
                    <materialDesign:PackIcon Kind="WindowMaximize" HorizontalAlignment="Center" VerticalAlignment="Center"  Foreground="White" Width="20" Height="20" />
                </Button>

                <Button  Style="{StaticResource MaterialDesignToolButton}" Command="{x:Static SystemCommands.RestoreWindowCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window,Mode=FindAncestor}}">
                    <materialDesign:PackIcon Kind="WindowRestore" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" Width="20" Height="20" />
                </Button>

                <Button  Style="{StaticResource MaterialDesignToolButton}" Command="{x:Static SystemCommands.CloseWindowCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window,Mode=FindAncestor}}">
                    <materialDesign:PackIcon Kind="WindowClose" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" Width="20" Height="20" />
                </Button>
            </StackPanel>
            <!--<StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,10,0">
                <Button Content="Minimize" Command="{Binding MinimizeCommand}" Width="80" Height="25" Margin="5,0"/>
                <Button Content="Maximize" Command="{Binding MaximizeCommand}" Width="80" Height="25" Margin="5,0"/>
                <Button Content="Close" Command="{Binding CloseCommand}" Width="80" Height="25" Margin="5,0"/>
            </StackPanel>-->
        </Grid>

        <StackPanel Grid.Row="1" Grid.RowSpan="2" Grid.Column="0" Orientation="Vertical"  >
            <StackPanel.Resources>
                <Style TargetType="materialDesign:PackIcon">
                    <Setter Property="Width" Value="25"/>
                    <Setter Property="Height" Value="25"/>
                    <Setter Property="VerticalAlignment" Value="Center" />
                </Style>

                <Style x:Key="NavigationMenuItem" TargetType="Button">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="16,12"/>
                    <!-- 左右 16，上下 12 -->
                    <Setter Property="Foreground" Value="#333"/>
                    <Setter Property="FontFamily" Value="{StaticResource MaterialDesignFont}"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>

                    <!-- 鼠标悬停效果 -->
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F5F5F5"/>
                            <Setter Property="Foreground" Value="#000"/>
                        </Trigger>

                        <!-- 选中状态（通过 IsSelected 绑定实现） -->
                        <!--<Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E8E8E8"/>
                            <Setter Property="Foreground" Value="#000"/>
                            <Setter Property="BorderBrush" Value="#BDBDBD"/>
                            <Setter Property="BorderThickness" Value="1"/>
                        </Trigger>-->
                    </Style.Triggers>
                </Style>
            </StackPanel.Resources>
            <Expander>
                <Expander.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" >
                            <materialDesign:PackIcon Kind="Menu" HorizontalAlignment="Left"  VerticalAlignment="Center"/>
                            <TextBlock Text="  Menu" VerticalAlignment="Center" FontSize="14" Padding="10"  FontWeight="SemiBold" />
                        </StackPanel>
                    </DataTemplate>
                </Expander.HeaderTemplate>
                <!--<StackPanel Margin="0,0,0,0" HorizontalAlignment="Stretch">
                    <Button Content="Nav-A" Command="{Binding NavigateCommand}" CommandParameter="ViewA" />
                    <Button Content="Nav-B" Command="{Binding NavigateCommand}" CommandParameter="ViewB"  Height="30"/>
                    <Button Content="DialogSevice" Command="{Binding DialogSeviceCommand}" CommandParameter="ViewB"  Height="30"/>
                    <Button Content="ActiveDeactive" Command="{Binding ActivationDeactivationCommand}" Height="30" />
                    <Button Content="CanExcute" Command="{Binding CanExcuteCommand}"  Height="25" />
                </StackPanel>-->
                <!--SelectedItem="{Binding SelectedMenuItem}"-->

                <!--<ListBox ItemsSource="{Binding DomainMenuItems}" >
                    <ListBox.ItemTemplate>
                        <DataTemplate >
                            <Button Content="{Binding Title}" Style="{StaticResource MaterialDesignFlatButton}" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" 
                                    VerticalAlignment="Stretch" VerticalContentAlignment="Stretch" Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=UserControl,Mode=FindAncestor}}" 
                                    CommandParameter="{Binding ViewName}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>-->
                <ItemsControl ItemsSource="{Binding DomainMenuItems}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!--<materialDesign:PackIcon Kind="{Binding SelectedIcon}" Foreground="Chocolate" Margin="70,0,0,0" />-->
                            <Button Content="{Binding Title}" Padding="10"  Style="{StaticResource MaterialDesignFlatButton}" Background="#F5F5F5" Height="47" VerticalAlignment="Center"
                                     VerticalContentAlignment="Center" Foreground="#333" Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=UserControl,Mode=FindAncestor}}" 
                                    CommandParameter="{Binding ViewName}">
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>
        </StackPanel>

        <!--<materialDesign:Transitioner SelectedIndex="0" Grid.Row="1" Grid.Column="1"  Transition="{x:Static materialDesign:TransitioningContentSlideAndFadeTransition}" Margin="0,64,0,0">
            <materialDesign:Transitioner.Items>
                -->
        <!-- 默认首页 -->
        <!--
                <materialDesign:TransitionerSlide>
                    <ContentControl prism:RegionManager.RegionName="MainViewRegion" />
                </materialDesign:TransitionerSlide>
            </materialDesign:Transitioner.Items>
        </materialDesign:Transitioner>-->

        <materialDesign:TransitioningContent Grid.Row="1"  Grid.Column="1"  Margin="0,0,0,0"  prism:RegionManager.RegionName="MainViewRegion" >
            <materialDesign:TransitioningContent.OpeningEffect>
                <materialDesign:TransitionEffect Kind="SlideInFromRight" Duration="0:0:0.6" />
                <!--<materialDesign:TransitionEffect Kind="FadeIn" />-->
            </materialDesign:TransitioningContent.OpeningEffect>
            <!--<materialDesign:TransitioningContent.ClosingEffect>
                <materialDesign:TransitionEffect Kind="SlideOutToLeft" />
                <materialDesign:TransitionEffect Kind="FadeOut" />
            </materialDesign:TransitioningContent.ClosingEffect>-->
            <!--<ContentControl prism:RegionManager.RegionName="MainViewRegion" />-->
        </materialDesign:TransitioningContent>

        <!--<UserControl Grid.Row="1" Grid.Column="1" prism:RegionManager.RegionName="MainViewRegion" core:RegionAnimation.Enable="True">

        </UserControl>-->
    </Grid>
</UserControl>
