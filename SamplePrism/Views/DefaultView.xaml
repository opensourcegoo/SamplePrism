<UserControl x:Class="SamplePrism.Views.DefaultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SamplePrism.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        
    </UserControl.Resources>
    <Grid Background="#FF364989" >
        <ScrollViewer HorizontalScrollBarVisibility="Disabled"  VerticalScrollBarVisibility="Auto">
            <ListBox>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="5"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="120" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0">
                            
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>

        <!--<Button Height="100"  Width="200" Content="123" Panel.ZIndex="2" SnapsToDevicePixels="True" Style="{StaticResource MaterialDesignFlatSecondaryButton}"/>
        <materialDesign:Card Padding="16"
                     Margin="10">
            <StackPanel>
                <TextBlock Text="设备状态" 
                   FontSize="18" 
                   Margin="0 0 0 8"/>
                <TextBlock Text="运行正常" FontSize="14"/>
            </StackPanel>
        </materialDesign:Card>-->

        <materialDesign:Card Padding="32">
            <StackPanel>
                <TextBox materialDesign:HintAssist.Hint="用户名"
                 materialDesign:TextFieldAssist.HasLeadingIcon="True"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}">
                    <!--<materialDesign:TextFieldAssist.LeadingIcon>
                        --><!--<materialDesign:PackIconKind Kind="Home"/>--><!--
                        <materialDesign:PackIcon Kind="Account"/>
                    </materialDesign:TextFieldAssist.LeadingIcon>-->
                </TextBox>

                <Button Content="登录" 
                Style="{StaticResource MaterialDesignRaisedButton}"
                Margin="0,16,0,0"/>
            </StackPanel>
        </materialDesign:Card>

        <!--<Image Width="200" Height="200" Source="/Images/logo_p.png" VerticalAlignment="Top" />-->

    </Grid>
</UserControl>
